package org.javasql;

public class Main {
    public static void main(String[] args) {
        LamdaDemo.show();
        LamdaDemoLE.show();
        LambdaDemoVC.show();
        LambdaDemo_methodref.show();
    }
}

// Functional Interface: Only one single abstract method
// A method that does not have its implementation or body
// You create a class to implement the interface

// When you want to avoid doing this you use
// anonymous Inner Classes

// Implement an IF only once and use it for a single purpose

// Lambda expressions are a cleaner way to get the same result
// than when you use anonymous inner classes

// They are like AF that we can pass around

